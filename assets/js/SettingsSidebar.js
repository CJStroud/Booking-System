/**
----------------------------------
Sidebar moving when in mobile view
----------------------------------
**/

$(function Load(){
  if ($(window).width() < 768) {
    $('#expand-sidebar').click(function NavigateSidebarClick() {

    });
  }
});
